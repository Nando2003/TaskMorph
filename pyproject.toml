[tool.poetry]
name = "TaskMorph"
version = "0.3"
description = "Methods that will help you in coding RPAs"
authors = ["Fernando Fontes <nandofontes30@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.8"
selenium = "^4.23.1"
pillow = "^10.4.0"
packaging = "^23.0"
undetected-chromedriver = { git = "https://github.com/Nando2003/nando-undetected-chromedriver.git", branch = "master" }

[tool.poetry.build]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

